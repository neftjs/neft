global.window = global;
global.Neft = require('./{{neftFileName}}');
require('lib/moduleCache').register();
process.on('message', function(msg) {
    if (msg === 'terminate') {
        process.exit();
    }
});
{{&appCode}}
