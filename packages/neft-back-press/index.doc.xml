<meta
  name="BackPress"
  category="Modules"
  description="Handles device's back action"
/>

<article title="Introduction">
  <p>Some of the devices can have 'back' button or a gesture which should show previous page.</p>

  <p>Using this module you can handle for such actions or finally close the app, if there is no previous page to go.</p>

  <p>Install it by typing <code>npm i @neft/back-press</code> and later use it by <code>const backPress = require('@neft/back-press')</code>.</p>
</article>

<signal name=onBackPress>
  <p>Is called any time, user want's to see previous page.</p>

  <script type="html">
    <script->
      const { onBackPress } = require('@neft/back-press')

      exports.default = () => ({
        handleBackPress() {
          // show previous screen
        },
        onRender() {
          onBackPress.connect(this.handleBackPress)
        },
        onRevert() {
          onBackPress.disconnect(this.handleBackPress)
        },
      })
    </script->
  </script>
</signal>

<method name=killApp>
  <p>Closes the app immediately.</p>

  <p>It's common pattern to close the app if there is no previous page to go.</p>
</method>
