# coffeelint: disable=no_debugger

'use strict'

module.exports = (impl) ->
    initDeviceNamespace: ->

    logDevice: (msg) ->
        console.log msg

    showDeviceKeyboard: ->

    hideDeviceKeyboard: ->
