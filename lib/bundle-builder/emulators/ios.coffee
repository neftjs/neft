'use strict'

module.exports = ->
    requestAnimationFrame: ->
    ios:
        postMessage: ->
    _neft:
        http:
            request: -> 0
            onResponse: ->
        native:
            transferData: ->
            onData: ->
