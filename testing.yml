browserHttpServer:
    protocol: http
    host: localhost
    port: 3003
server:
    protocol: http
    host: localhost
    port: 3004
environments:
    -
        platform: local
    -
        platform: node
        version: current
    -
        platform: browser
        browser: chrome
        enabled: $TEST_CHROME
        width: 200
        height: 200
    -
        platform: webgl
        browser: chrome
        enabled: $TEST_CHROME
        width: 200
        height: 200
