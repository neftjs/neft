server:
    protocol: http
    host: localhost
    port: 3002
environments:
    -
        platform: local
    -
        platform: node
        version: current
    -
        platform: node
        version: 0.11.0
        enabled: $TEST_NVM
    -
        platform: node
        version: 4.1.2
        enabled: $TEST_NVM
    -
        platform: node
        version: 5.0.0
        enabled: $TEST_NVM
    -
        platform: node
        version: 6.2.2
        enabled: $TEST_NVM
    -
        platform: node
        version: 7.0.0
        enabled: $TEST_NVM
    -
        platform: browser
        browser: chrome
        enabled: $TEST_CHROME
        width: 200
        height: 200
