version: 2
plugins:
    coffeelint:
        enabled: true
        config: coffeelint.json
    fixme:
        enabled: true
    tailor:
        enabled: true
    shellcheck:
        enabled: true
    duplication:
        enabled: false

